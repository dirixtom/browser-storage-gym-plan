{
  "name": "kilo-e2e",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end tests: real browser + real Fastify server + real Postgres.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@fastify/cookie": "^9.3.1",
    "@playwright/test": "^1.49.0",
    "pg": "^8.12.0"
  }
}
